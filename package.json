{
  "name": "uzumaki",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "pnpm build:core && pnpm copy:binary && pnpm --filter playground start",
    "copy:binary": "bun scripts/copy-binary.ts --release",
    "build:core": "cargo build --release -p uzumaki",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
